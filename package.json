{
  "name": "bin-which",
  "version": "0.0.6",
  "description": "node equivalent of the `which` command",
  "author": "Ruben Schmidmeister <ruben.schmidmeister@icloud.com>",
  "homepage": "https://github.com/bash/node-bin-which",
  "license": "MIT",
  "dependencies": {},
  "bin": {
    "which": "which.js"
  },
  "scripts": {
    "test": "mocha",
    "coverage": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "files": [
    "README.md",
    "which.js",
    "lib/which.js"
  ],
  "bugs": {
    "url": "https://github.com/bash/node-bin-which/issues",
    "email": "ruben.schmidmeister@icloud.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bash/node-bin-which.git"
  },
  "devDependencies": {
    "coveralls": "^2.11.6",
    "istanbul": "^0.4.1",
    "mocha": "^2.3.4",
    "mocha-lcov-reporter": "^1.0.0"
  }
}
